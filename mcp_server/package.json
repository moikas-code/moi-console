{
  "name": "mcp_server",
  "version": "0.0.1",
  "description": "Bun-based MCP server with dynamic tool loading and stdio transport.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "start": "bun run src/index.ts",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.11.0"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "eslint": "^9.8.0"
  }
} 